import { theme } from "@/styles/theme";
import { PiUsersThreeLight } from "react-icons/pi";

export default function UsersIcon() {
  return <PiUsersThreeLight size={20} color={theme.colors.primary[200]} />;
}
